% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_search.R
\name{get_lav_lrt}
\alias{get_lav_lrt}
\title{Compute 1-df Likelihood Ratio Tests for Equality Constraints}
\usage{
get_lav_lrt(x, ind, op = c("=~", "~1", "~~", "|"))
}
\arguments{
\item{x}{A fitted lavaan object.}

\item{ind}{Character vector of indicator names to consider.}

\item{op}{Character string specifying the operator type (\code{"=~"}, \code{"~1"}, \code{"~~"}, or \code{"|"}).}
}
\value{
A data frame with one row for each tied equality constraint,
containing \code{id}, \code{lhs}, \code{op}, \code{rhs}, \code{group}, \code{plabel}, the 1-df LRT
statistic for releasing the constraint (\code{mi}), and its p-value (\code{p}).
For \code{op = "~1"} and \code{op = "|"}, \code{rhs} is empty and the variable is in
\code{lhs}. A data frame with 0 rows (and the same column names) is
returned when no candidate is tied.
}
\description{
Computes 1-df likelihood ratio tests (LRTs) for releasing equality
constraints on specific parameters, where each tied constraint is
released individually and the model is refit.
}
\examples{
library(lavaan)
# Indicator matrix
spec <- matrix(c(
    "y1", "y2", "y3", "y4",
    "y5", "y6", "y7", "y8"
), ncol = 2)
# Scalar invariance
fit <- longcfa(spec,
               lv_names = c("dem60", "dem65"),
               data = PoliticalDemocracy,
               long_equal = c("loadings", "intercepts"))
# 1-df LRTs for releasing each tied intercept constraint
get_lav_lrt(fit, ind = paste0("y", 1:8), op = "~1")
}
